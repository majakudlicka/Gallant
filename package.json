{
  "name": "maya-compiler",
  "version": "1.0.0",
  "description": "",
  "main": "build/lexer/lexer.js",
  "dependencies": {
    "babel-preset-es2015": "^6.1.18",
    "emoji-regex": "8.0.0",
    "node-emoji": "1.10.0"
  },
  "devDependencies": {
    "babel-cli": "^6.2.0",
    "eslint": "6.8.0",
    "eslint-config-airbnb-base": "14.0.0",
    "eslint-plugin-import": "2.20.0",
    "mocha": "^2.3.4"
  },
  "scripts": {
    "build": "babel src --source-maps --out-dir build",
    "test": "babel src --source-maps --out-dir build && mocha --recursive build/test/",
    "repl": "babel src --source-maps --out-dir build && node build/run-repl.js",
    "load": "babel src --source-maps --out-dir build && node build/load-file.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/majakudlicka/Interpreter.git"
  },
  "author": "",
  "license": "ISC",
  "homepage": "https://github.com/majakudlicka/Interpreter#readme"
}
